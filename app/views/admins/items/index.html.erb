<% @items.each do |item| %>
 <p><%= link_to item.item_name, admins_item_path(item.id) %></p>
 <p><%= item.price %></p>
 <p><%= item.genre %></p>
 <p><%= item.sales_status_text %></p>
<% end %>
<%= link_to 'Topへ', admins_root_path %>